# === 資料庫設定 ===

# 本地開發 - 使用 Docker PostgreSQL（推薦）
DATABASE_URL="postgresql://tattoo_user:tattoo_password@localhost:5432/tattoo_crm_dev"

# 生產環境 - Railway 自動提供（請在 Railway Variables 中設定）
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# === 認證設定 ===

# JWT 密鑰
# 注意：後端程式碼使用「Access / Refresh」兩組密鑰
# 生產環境請使用強隨機字串（建議 64 bytes 以上）
# 生成方法: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET="local-dev-access-secret-key"
JWT_REFRESH_SECRET="local-dev-refresh-secret-key"

# JWT 過期時間（可選，不填會使用程式預設值：15m / 7d）
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL="7d"

# === 應用程式設定 ===

# 執行環境
NODE_ENV="development"

# 伺服器埠號
PORT=4000

# === CORS 設定 ===

# 允許的前端來源，多個來源用逗號分隔
CORS_ORIGIN="http://localhost:3000,http://localhost:4001"

# 生產環境範例:
# CORS_ORIGIN="https://your-frontend.railway.app,https://your-domain.com"

# === 數據種子設定 ===

# 保護真實數據 - 重建數據時是否保留分店和刺青師
# true: 保護模式，保留現有分店和刺青師，只重建測試數據（用戶、預約、訂單等）
# false: 完整重建模式，重建所有數據（包括分店和刺青師）
PROTECT_REAL_DATA="false"

# 生產環境建議設為 true:
# PROTECT_REAL_DATA="true"

