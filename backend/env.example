# === 資料庫設定 ===

# 本地開發 - 使用 Docker PostgreSQL（推薦）
DATABASE_URL="postgresql://tattoo_user:tattoo_password@localhost:5432/tattoo_crm_dev"

# 生產環境 - Railway 自動提供（請在 Railway Variables 中設定）
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# === 認證設定 ===

# JWT 密鑰 - 本地開發可用簡單值，生產環境請使用強隨機字串（32+ 字元）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="local-dev-secret-key"

# === 應用程式設定 ===

# 執行環境
NODE_ENV="development"

# 伺服器埠號
PORT=4000

# === CORS 設定 ===

# 允許的前端來源，多個來源用逗號分隔
CORS_ORIGIN="http://localhost:3000,http://localhost:4001"

# 生產環境範例:
# CORS_ORIGIN="https://your-frontend.railway.app,https://your-domain.com"

