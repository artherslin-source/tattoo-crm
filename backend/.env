# ✅ 伺服器監聽埠（NestJS 啟動端口）
PORT=4000

# ✅ Node 環境設定
NODE_ENV=production

# ✅ 後端應用名稱（可自訂，用於辨識）
APP_NAME=tattoo-crm-backend

# ✅ Railway 預設會自動注入資料庫連線參數（以下格式為 Prisma 專用）
# ⚠️ 你需要進入 Railway 的 “Variables” 區，將這行設定為實際的 DATABASE_URL
# 通常 Railway 自動產生的會長這樣（請照樣填入）：
# postgresql://postgres:<你的密碼>@containers-us-west-90.railway.app:7941/railway
DATABASE_URL=postgresql://postgres:<YOUR_DB_PASSWORD>@containers-us-west-90.railway.app:7941/railway?schema=public

# ✅ JWT 金鑰（若你的系統有登入認證功能）
JWT_SECRET=supersecretkey

# ✅ CORS 允許來源（前端部署網址）
CORS_ORIGIN=https://tattoo-crm.vercel.app

# ✅ Prisma 設定（固定）
PRISMA_CLIENT_ENGINE_TYPE=binary

# ✅ 後端 API URL（前端若需要環境變數抓 API 用）
API_URL=https://tattoo-crm-api.up.railway.app

# ✅ 資料庫連線逾時與重試設定（非必填，但建議）
DATABASE_CONNECTION_TIMEOUT=5000
DATABASE_RETRY_COUNT=5