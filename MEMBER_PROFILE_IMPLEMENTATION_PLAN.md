# 會員個人資料頁面實施計劃

**創建日期：** 2025-01-06  
**狀態：** 🚀 **進行中**

---

## 📋 功能規劃

### 一、基本資料區（Member Info）

**顯示內容：**
- ✅ 頭像上傳和顯示
- ✅ 暱稱、姓名編輯
- ✅ 電子郵件、手機號碼
- ✅ 會員編號、註冊日期、最後登入時間
- ✅ 會員級別（一般會員/VIP/旗艦會員）
- ✅ 身分驗證狀態

### 二、互動記錄區（Member Activity）

**子功能：**
1. **我的預約** - 過去與未來預約清單
2. **我的作品收藏** - 收藏的刺青師作品
3. **我的付款記錄** - 訂金/尾款/消費明細
4. **我的回饋與評價** - 評論、星級、照片
5. **我的諮詢記錄** - 聊天記錄摘要

### 三、個人化設定區（Preferences & Settings）

**設定項目：**
1. **通知設定** - Email/Line/App推播
2. **個人偏好** - 風格、顏色、喜好師傅
3. **隱私設定** - 公開/私密設定
4. **安全設定** - 修改密碼、登入裝置
5. **帳號綁定** - LINE/Google/Apple/Facebook
6. **資料管理** - 資料匯出、刪除帳號

---

## 🎨 介面設計

### 布局結構

```
┌─────────────────────────────────────────┐
│ Header (頂部導航)                       │
├──────────┬──────────────────────────────┤
│          │                              │
│ 左側導航  │  右側內容區                   │
│          │                              │
│ 👤 我的資料│  [根據選擇的項目顯示不同內容]  │
│ 📅 預約紀錄│                              │
│ 🖼 收藏作品│                              │
│ 💰 付款記錄│                              │
│ ⭐ 我的評價│                              │
│ ⚙️ 設定中心│                              │
│ 🔒 安全隱私│                              │
│          │                              │
└──────────┴──────────────────────────────┘
```

### 響應式設計

**桌面版：** 左側固定導航 + 右側內容  
**平板/手機：** 頂部Tab導航 + 內容區

---

## 🗄️ 數據庫擴展需求

### Member 模型擴展

需要添加的欄位：
```prisma
model Member {
  // 現有欄位...
  
  // 新增欄位
  nickname          String?    // 暱稱
  photoUrl          String?    // 頭像
  lineId            String?    // LINE 綁定
  googleId          String?    // Google 綁定
  appleId           String?    // Apple 綁定
  facebookId        String?    // Facebook 綁定
  
  isVerified        Boolean @default(false)  // 身分驗證
  
  // 通知設定
  emailNotification Boolean @default(true)
  lineNotification  Boolean @default(false)
  appNotification   Boolean @default(true)
  
  // 個人偏好
  preferredStyles   String[]   // 喜好風格
  preferredColors   String[]   // 喜好顏色
  favoriteArtistIds String[]   // 喜好師傅
  
  // 隱私設定
  showPortfolio     Boolean @default(true)  // 公開收藏
  showReviews       Boolean @default(true)  // 公開評價
  
  lastLoginAt       DateTime?  // 最後登入
}
```

### PortfolioFavorite 模型（新增）

```prisma
model PortfolioFavorite {
  id         String   @id @default(cuid())
  userId     String
  portfolioId String
  createdAt  DateTime @default(now())
  
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  portfolio  Portfolio @relation(fields: [portfolioId], references: [id], onDelete: Cascade)
  
  @@unique([userId, portfolioId])
}
```

### Review 模型（新增）

```prisma
model Review {
  id            String   @id @default(cuid())
  userId        String
  artistId      String
  appointmentId String?
  rating        Int      // 1-5 星
  comment       String?
  photos        String[] // 照片 URLs
  isPublic      Boolean  @default(true)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  user          User        @relation(fields: [userId], references: [id])
  artist        Artist      @relation(fields: [artistId], references: [id])
  appointment   Appointment? @relation(fields: [appointmentId], references: [id])
}
```

---

## 🔧 實施階段

### 階段 1：基礎架構（進行中）
- [ ] 創建頁面路由 `/profile`
- [ ] 設計左側導航組件
- [ ] 設計響應式布局
- [ ] 創建基本資料展示組件

### 階段 2：基本資料區
- [ ] 顯示會員基本信息
- [ ] 頭像上傳功能
- [ ] 編輯個人資料表單
- [ ] 後端 API：更新會員資料

### 階段 3：我的預約
- [ ] 整合現有的 /appointments 頁面
- [ ] 添加過濾和排序功能
- [ ] 預約詳情查看

### 階段 4：收藏和評價
- [ ] 作品收藏功能
- [ ] 收藏列表展示
- [ ] 評價系統實現

### 階段 5：設定中心
- [ ] 通知偏好設定
- [ ] 個人喜好設定
- [ ] 隱私設定
- [ ] 安全設定

### 階段 6：進階功能
- [ ] 帳號綁定（LINE/Google等）
- [ ] 資料匯出
- [ ] 帳號刪除流程

---

## 📂 文件結構

```
frontend/src/
├── app/
│   └── profile/
│       ├── page.tsx (主頁面)
│       ├── layout.tsx (布局)
│       ├── info/
│       │   └── page.tsx (基本資料)
│       ├── appointments/
│       │   └── page.tsx (我的預約)
│       ├── favorites/
│       │   └── page.tsx (收藏作品)
│       ├── payments/
│       │   └── page.tsx (付款記錄)
│       ├── reviews/
│       │   └── page.tsx (我的評價)
│       └── settings/
│           ├── page.tsx (設定中心)
│           ├── notifications/page.tsx (通知設定)
│           ├── preferences/page.tsx (個人偏好)
│           ├── privacy/page.tsx (隱私設定)
│           └── security/page.tsx (安全設定)
└── components/
    └── profile/
        ├── ProfileSidebar.tsx (側邊導航)
        ├── ProfileHeader.tsx (個人資料頭部)
        ├── AvatarUpload.tsx (頭像上傳)
        ├── MemberInfoCard.tsx (會員資訊卡片)
        └── ...
```

---

## 🚀 開始實施

### 優先級

**第一優先（MVP）：**
1. 基本資料頁面布局 ✅ 開始
2. 顯示會員基本信息
3. 我的預約記錄（整合現有）
4. 基本編輯功能

**第二優先：**
1. 頭像上傳
2. 付款記錄查看
3. 通知設定

**第三優先：**
1. 收藏功能
2. 評價系統
3. 帳號綁定
4. 資料匯出

---

**🚀 開始實施會員個人資料頁面！** 

同時，Railway 的 TypeScript 錯誤已全部修復，正在等待部署...

